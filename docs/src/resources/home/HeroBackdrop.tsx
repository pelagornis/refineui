/** Minimal hero backdrop — single soft glow. */
export function HeroBackdrop() {
    return (
        <div data-refineui-home-backdrop aria-hidden>
            <span data-refineui-home-glow />
        </div>
    );
}
