---
title: Menu
description: Menu panel with nested submenus
---

import MenuPreview from "../../../components/MenuPreview";

# Menu

Menu panel with a list of items. Matches Web Kit **Menu** `633:4268`. Use [Dropdown](/components/dropdown/) when you need a compact trigger menu; use **Menu** for the wider panel composition (icons, descriptions, shortcuts, nested menus).

## Install & import

```tsx
import {
  Button,
  Menu,
  MenuDivider,
  MenuItem,
  MenuList,
  MenuPopover,
  MenuSection,
  MenuSub,
  MenuSubContent,
  MenuSubTrigger,
  MenuTrigger,
} from "@refineui/react";
import "@refineui/react/refineui.css";
```

## Basic usage

```tsx
<Menu positioning={{ autoSize: true }}>
  <MenuTrigger>
    <Button>Toggle menu</Button>
  </MenuTrigger>
  <MenuPopover>
    <MenuList>
      <MenuSection>Edit</MenuSection>
      <MenuItem shortcut="⌘C">Copy</MenuItem>
      <MenuDivider />
      <MenuSub>
        <MenuSubTrigger>Preferences</MenuSubTrigger>
        <MenuSubContent>
          <MenuItem>General</MenuItem>
          <MenuItem>Appearance</MenuItem>
        </MenuSubContent>
      </MenuSub>
    </MenuList>
  </MenuPopover>
</Menu>
```

Chevron on `MenuSubTrigger` is built-in (Web Kit `endIcon`). Prefer `MenuSub` over a plain `MenuItem` with a chevron `endIcon` when the row opens a nested panel.

## Preview

<div class="not-content">
  <MenuPreview client:load />
</div>

## Props

| Prop | Type | Description |
|------|------|-------------|
| `positioning` | `{ autoSize?: boolean }` | Optional auto-width for the popover |

Compound API: `MenuTrigger`, `MenuPopover`, `MenuList`, `MenuItem`, `MenuSub`, `MenuSubTrigger`, `MenuSubContent`, …

`MenuItem` slots: `startIcon`, `description`, `shortcut`, `endIcon`.
