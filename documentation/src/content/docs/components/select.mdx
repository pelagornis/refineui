---
title: Select
description: 커스텀 선택 필드 — Figma Web Kit Select 스타일 반영
---

import SelectPreview from "../../../components/SelectPreview";

# Select

Figma Web Kit `Select` (`1116:1429`, `1117:1177`, `1144:2410`, `1144:2756`) 스타일을 반영하며, 동작은 `@radix-ui/react-select`를 그대로 사용합니다.

## 설치·임포트

```tsx
import {
  Select,
  SelectTrigger,
  SelectValue,
  SelectPortal,
  SelectContent,
  SelectItem,
  SelectSection,
} from "@refineui/react";
import "@refineui/react/refineui.css";
```

## 기본 사용

`@radix-ui/react-select`와 동일하게 메뉴는 `SelectPortal` 안에 `SelectContent`를 둡니다.

```tsx
<Select placeholder="국가 선택" onValueChange={(next) => console.log(next)}>
  <SelectTrigger>
    <SelectValue placeholder="선택하세요" />
  </SelectTrigger>
  <SelectPortal>
    <SelectContent>
      <SelectSection>Asia</SelectSection>
      <SelectItem value="kr">Korea Standard Time</SelectItem>
      <SelectItem value="jp">Japan Standard Time</SelectItem>
    </SelectContent>
  </SelectPortal>
</Select>;
```

## Preview

<div class="not-content">
  <SelectPreview client:load />
</div>

## Props

| Prop | 타입 | 기본값 | 설명 |
|------|------|--------|------|
| `placeholder` | `string` | — | 미선택 상태 라벨 |
| `size` | `"sm"` \| `"md"` \| `"lg"` | `"md"` | 필드 높이·패딩·타이포 |
| `fullWidth` | `boolean` | `false` | 너비 100% |
| `disabled` | `boolean` | `false` | 비활성화 |
| `value` | `string` | — | 제어 값 |
| `defaultValue` | `string` | — | 비제어 초기값 (`undefined`면 미선택) |
| `open` | `boolean` | — | 제어 열림 상태 |
| `onOpenChange` | `(open: boolean) => void` | — | 열림 상태 변경 |
| `onValueChange` | `(value: string) => void` | — | 선택 값 변경 |

## Composition

- `SelectTrigger` — 필드 트리거
- `SelectValue` — 현재 선택 라벨/placeholder
- `SelectPortal` — 메뉴를 `document.body`(또는 `container`)로 포털
- `SelectContent` — 메뉴 패널
- `SelectGroup` — 그룹
- `SelectSection` — 섹션 제목
- `SelectItem` — 선택 항목

