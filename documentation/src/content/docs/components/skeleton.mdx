---
title: Skeleton
description: Loading placeholder — rectangle / circle
---

import SkeletonPreview from "../../../components/SkeletonPreview";

# Skeleton

Placeholder while content loads. Solid fill with a soft **opacity pulse** (same idea as shadcn / MUI), using alias surface tokens — not a sliding shimmer.

## Install & import

```tsx
import { Skeleton } from "@refineui/react";
import "@refineui/react/refineui.css";
```

## Basic usage

```tsx
<Skeleton width="100%" height={20} />
<Skeleton shape="circle" width={40} />
```

## Preview

<div class="not-content">
  <SkeletonPreview client:load />
</div>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `width` | `number` \| `string` | — | Width |
| `height` | `number` \| `string` | — | Height |
| `shape` | `"rectangle"` \| `"circle"` | `"rectangle"` | Circle is typical for avatars |

## Examples

### Card list skeleton

```tsx
<div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
  <Skeleton height={16} width="60%" />
  <Skeleton height={12} width="100%" />
  <Skeleton height={12} width="90%" />
</div>
```
