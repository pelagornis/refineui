---
title: Tabs
description: 탭 컴포넌트
---

import TabsDemo from "../../../components/TabsDemo";

# Tabs

탭 컴포넌트입니다. Preview에서 **pill**(기본)과 **underline** 변형을 함께 볼 수 있습니다.

## Preview

<div class="not-content">
  <TabsDemo client:load />
</div>

## Props

- `items` — TabItem[] (id, label, content)
- `variant` — `pill`(기본) | `underline`
- `defaultTab` — 초기 선택 탭 id
- `onChange` — 탭 변경 시 `(id: string) => void`
