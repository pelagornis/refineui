/**
 * Web Kit component sizes — re-export from `@refineui/tokens`.
 * Values are Foundation aliases (`componentSizeFoundationKeys`); do not add raw px here.
 */
export {
    componentSizes,
    componentSizeFoundationKeys,
    foundationSizes,
    type ComponentSizeName,
    type FoundationSizeName,
} from "@refineui/tokens";
