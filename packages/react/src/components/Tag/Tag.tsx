/** Web Kit COMPONENT_SET `Tag` `574:6578` — 구현은 `Chip`과 동일 (`Chip`으로도 export). */
export {
    Chip as Tag,
    type ChipProps as TagProps,
    type ChipVariant as TagVariant,
    type ChipSize as TagSize,
} from "../Chip";
