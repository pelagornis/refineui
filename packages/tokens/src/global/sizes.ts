/**
 * Icon glyph sizes (px) — **not tied to font/typography tokens** (explicit values only).
 */
export const iconSizes = {
    xxsmall: 12,
    xsmall: 16,
    small: 20,
    medium: 24,
    large: 28,
    xlarge: 32,
    xxlarge: 48,
} as const;
